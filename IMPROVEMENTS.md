- 2026-06-13: fix boot failure on :testing — reverted buildah commit to podman build --squash-all; chunka composefs xattr injection requires flat single-layer input ([#841](https://github.com/projectbluefin/dakota/issues/841), [#846](https://github.com/projectbluefin/dakota/pull/846))
- 2026-06-13: add composefs file-capability and GDM boot regression tests to testsuite smoke suite; gate :testing publish in dakota (publish.yml smoke job) and bluefin-lts (post-merge-e2e.yml + publish_stream_tag: false) ([testsuite#433](https://github.com/projectbluefin/testsuite/pull/433))
- 2026-06-14: unblock `testing` pipeline — fixed `testing` branch divergence (manual reset + PR #237 to actions for force-reset sync), merged PRs #763/#834/#837, fixed pr-triage auto-merge gap (#858), added `validate` required check to branch protection
- 2026-06-14: fix boot-check gate (never passed since PR #849): (1) `[install.filesystem.root] type = "xfs"` required in bootc install config (#859); (2) drop `--via-loopback`, pre-create loop on host so partition nodes are visible to container at mkfs time (#864)
- 2026-06-16: fix boot-check gate (still never passed — two compounding udev races): (1) `losetup -P` (PARTSCAN) so kernel tracks partitions when sfdisk writes inside container (#883); (2) pre-seed GPT partition table before bootc so node creation is a synchronous update, not async create (#889)
