name: Validate Renovate Config
# Thin caller — logic lives in projectbluefin/actions/reusable-validate-renovate.yml

on:
  pull_request:
    paths:
      - ".github/renovate.json5"
  push:
    branches:
      - main
    paths:
      - ".github/renovate.json5"

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: projectbluefin/actions/.github/workflows/reusable-validate-renovate.yml@2e8139cf6abc3663aa39e358dc43492bd9fe48d5 # v1
