name: Validate Renovate Config
# Thin caller — logic lives in projectbluefin/actions/reusable-validate-renovate.yml

on:
  pull_request:
    paths:
      - ".github/renovate.json5"
  push:
    branches:
      - main
    paths:
      - ".github/renovate.json5"

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: projectbluefin/actions/.github/workflows/reusable-validate-renovate.yml@3f01fd8e8c186b072c21d0453f2dc66c727f9db8 # v1
