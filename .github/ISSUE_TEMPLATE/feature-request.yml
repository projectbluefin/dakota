name: "Feature request"
description: "Propose something new. Specific proposals ship. Vague ones wait."
labels: ["kind/enhancement", "status/discussing"]
type: "Feature"
body:
  - type: markdown
    attributes:
      value: |
        ## Humans decide what gets built. Contributors build it.

        This is not a ticket system — it is a conversation between you and the people building Dakota.
        A maintainer will read this, decide if it fits the project's direction, and approve it.
        Once approved, a contributor or agent can claim and ship it.

        **The bottleneck is almost never implementation. It is clarity.**

        Contributors can implement well-scoped work fast — sometimes the same day. But they can only
        act on what is clearly defined.

        **Ask yourself before submitting:**
        - Can someone read this and know exactly what file to change? Remember LLMs are non-deterministic
        - Does "done" have a clear finish line — a command to run, a thing to see?
        - Is this specific to Dakota, or would it be better filed upstream (GNOME, fdsdk, bootc)?

  - type: markdown
    attributes:
      value: |
        > **How this gets built:**
        > ```
        > filed ──▶ discussing ──▶ approved ──▶ queued ──▶ claimed ──▶ done
        >        [status/         [status/     [status/   [status/
        >         discussing]      approved]    queued]    claimed]
        > ```
        > Your proposal enters discussion. A maintainer approves it when the spec is clear.
        > Then a contributor or agent `/claim`s it and builds it.

  - type: textarea
    id: problem
    attributes:
      label: "What would you like to see?"
      description: "Plain language. What is missing, broken, or annoying? Be specific — name the command, the file, the workflow step."
      placeholder: |
        Add this feauture or ask for a design review
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: "What does done look like?"
      description: "Describe the end state. What command do you run? What do you see? What file changes? A concrete finish line is what allows this to be implemented."
      placeholder: |
        "I should be able to setup Docker in one command"
    validations:
      required: true

  - type: dropdown
    id: area
    attributes:
      label: "Affected area"
      description: "Where does this change live? Helps route it to the right epic and the right contributor."
      options:
        - "just recipes / ujust"
        - "GNOME / desktop"
        - "bootc / image updates"
        - "install / ISO"
        - "nvidia"
        - "flatpak / apps"
        - "brew / CLI tools"
        - "build system / BST"
        - "CI / automation"
        - "hardware / drivers"
        - "other"
    validations:
      required: true

  - type: textarea
    id: extra
    attributes:
      label: "Extra context"
      description: "Optional - The more you add here, the less back-and-forth before this reaches Ready."
      placeholder: "related issues, upstream references, or files you think are affected"
    validations:
      required: false

  - type: markdown
    attributes:
      value: |
        ---
        A maintainer will discuss and approve if this fits the project direction. Once approved,
        it enters the build queue:

        ```
        filed → discussed → approved → queued → claimed → done
        ```

        Once it ships, the community verifies it with `ujust verify <issue-number>`.
